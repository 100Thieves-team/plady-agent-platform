#!/bin/bash
set -euxo pipefail

exec > >(tee -a /var/log/llm-wiki-bootstrap.log | logger -t llm-wiki-bootstrap -s 2>/dev/console) 2>&1

APP_DIR="${app_dir}"
DOCKER_COMPOSE_VERSION="${docker_compose_version}"

# Base packages and Docker daemon.
dnf update -y
dnf install -y docker git curl ca-certificates
systemctl enable --now docker
usermod -aG docker ec2-user || true

# GitHub CLI is convenient for manual private-repo login, but deployment should
# not fail if this optional install has a transient issue.
if dnf install -y dnf-plugins-core && dnf config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo; then
  dnf install -y gh || echo "GitHub CLI install failed; continuing without gh."
else
  echo "GitHub CLI repo setup failed; continuing without gh."
fi

# Docker Compose v2 plugin.
ARCH="$(uname -m)"
case "$ARCH" in
  x86_64) COMPOSE_ARCH="x86_64" ;;
  aarch64|arm64) COMPOSE_ARCH="aarch64" ;;
  *) echo "Unsupported architecture: $ARCH" >&2; exit 1 ;;
esac
mkdir -p /usr/local/lib/docker/cli-plugins
curl -fsSL "https://github.com/docker/compose/releases/download/v$${DOCKER_COMPOSE_VERSION}/docker-compose-linux-$${COMPOSE_ARCH}" \
  -o /usr/local/lib/docker/cli-plugins/docker-compose
chmod +x /usr/local/lib/docker/cli-plugins/docker-compose
ln -sf /usr/local/lib/docker/cli-plugins/docker-compose /usr/local/bin/docker-compose

# Prepare an empty directory owned by ec2-user. Log in to GitHub and clone here
# manually after the instance is ready.
mkdir -p "$${APP_DIR}"
chown ec2-user:ec2-user "$${APP_DIR}"

cat >/home/ec2-user/LLM_WIKI_DEPLOY.md <<DOC
# Manual LLM Wiki deploy

This instance already has Docker, Docker Compose, Git, and optionally gh installed.

Example:

\`\`\`bash
gh auth login
git clone https://github.com/100Thieves-team/100Thieves-wiki-mcp.git $${APP_DIR}
cd $${APP_DIR}
docker compose up -d --build
docker compose ps
\`\`\`

If you do not use gh, clone with your preferred HTTPS token or SSH key setup.
DOC

chown ec2-user:ec2-user /home/ec2-user/LLM_WIKI_DEPLOY.md
