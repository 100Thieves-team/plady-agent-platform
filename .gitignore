.DS_Store

# Local llm-wiki data repo created by Docker Compose.
# It is its own git-backed wiki space, separate from this wrapper repo.
wiki-workspace/

# Local deploy keys generated while following the EC2 setup guide.
.deploy-keys/

# Terraform local state and provider cache
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
!*.tfvars.example

# Local/editor noise
.idea/
.vscode/*.log
